# size_type

```cpp
// fixtures::Other::size_type
#include "fixtures/NestedClassFixtures.h"
using size_type = uint16_t
```

A size type scoped to Other (should NOT collide with Outer::size_type in knownTypes).
