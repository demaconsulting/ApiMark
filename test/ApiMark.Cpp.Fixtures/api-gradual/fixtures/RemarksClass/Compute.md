# RemarksClass.Compute

```cpp
// fixtures::RemarksClass::Compute
int Compute()
```

Computes a result.

Uses an iterative algorithm to compute the result. May be called multiple times safely.

## Returns

The computed integer result.
