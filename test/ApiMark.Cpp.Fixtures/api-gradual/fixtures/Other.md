# Other

```cpp
// fixtures::Other
#include "fixtures/NestedClassFixtures.h"
```

Another class that also declares a size_type alias (different from Outer::size_type).

## Type Aliases

| Alias | Underlying Type | Description |
| --- | --- | --- |
| [size_type](Other/size_type.md) | uint16_t | A size type scoped to Other (should NOT collide with Outer::size_type in knownTypes). |
